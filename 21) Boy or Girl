#include <bits/stdc++.h>
using namespace std;
int main(){
    string s; cin>>s;
    sort(s.begin(), s.end());
    int cnt=unique(s.begin(), s.end())-s.begin();
    cout<<(cnt%2?"IGNORE HIM!":"CHAT WITH HER!");
}
