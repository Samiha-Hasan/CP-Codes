#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,k; cin >> n >> k;
    int cnt = 0, x;
    for(int i=0;i<n;i++){
        cin >> x;
        if(x + k <= 5) cnt++;
    }
    cout << cnt / 3;
}
