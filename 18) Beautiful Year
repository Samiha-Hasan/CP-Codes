#include <bits/stdc++.h>
using namespace std;
bool ok(int y){
    string s = to_string(y);
    sort(s.begin(), s.end());
    return unique(s.begin(), s.end()) == s.end();
}
int main(){
    int y; cin>>y;
    while(true){
        y++;
        if(ok(y)){ cout<<y; return 0; }
    }
}
