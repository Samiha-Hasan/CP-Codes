#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    string s,prev=""; int groups=0;
    while(n--){
        cin>>s;
        if(s!=prev) groups++;
        prev=s;
    }
    cout<<groups;
}
