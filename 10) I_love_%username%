#include <bits/stdc++.h>
using namespace std;
int main(){
    int n; cin>>n;
    int a; cin>>a;
    int mn=a,mx=a,ans=0;
    for(int i=1;i<n;i++){
        cin>>a;
        if(a>mx) ans++, mx=a;
        else if(a<mn) ans++, mn=a;
    }
    cout<<ans;
}
