#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,k; cin>>n>>k;
    int time=0, cnt=0;
    for(int i=1;i<=n;i++){
        time+=5*i;
        if(time+k>240) break;
        cnt++;
    }
    cout<<cnt;
}
