#include <bits/stdc++.h>
using namespace std;

int main(){
    int t; cin >> t;
    while(t--){
        vector<int>a(3);
        for(int &x:a) cin >> x;
        sort(a.begin(), a.end());
        cout << a[1] << "\n";
    }
}
